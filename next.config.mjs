/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos'], // Ajoutez ici les hôtes d'images que vous utilisez dans votre application
  },
};

export default nextConfig;
